import 'package:flutter/material.dart';

class VerticalSpaceMediumWidget extends StatelessWidget {
  const VerticalSpaceMediumWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 8);
  }
}
