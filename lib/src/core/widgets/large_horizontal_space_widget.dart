import 'package:flutter/material.dart';

class LargeHorizontalSpaceWidget extends StatelessWidget {
  const LargeHorizontalSpaceWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox(width: 16);
  }
}
